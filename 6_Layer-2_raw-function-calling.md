## 6. Layer 2 - Manual JSON Schemas vs LangChain Tool Abstraction

- About tools:
  - Without the `@tool` decorator, we have to create JSON schemas for the tools (for Ollama)
  - We checked other model, there are other JSON schemas etc

- We can see there are many abstractions / parametrization, such as: human message or user message etc
